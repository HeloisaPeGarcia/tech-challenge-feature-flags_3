apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
  labels:
    app: analytics-service
spec:
  ports:
    - port: 8005
      targetPort: 8005
      protocol: TCP
  selector:
    app: analytics-service
  type: ClusterIP

