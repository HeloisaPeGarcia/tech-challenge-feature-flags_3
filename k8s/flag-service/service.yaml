apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: togglemaster
  labels:
    app: flag-service
spec:
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
  selector:
    app: flag-service
  type: ClusterIP

